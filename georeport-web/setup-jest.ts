// Zoneless: this scaffold has no zone.js dependency (Angular 20+ CLI
// default with signals/standalone components).
import { setupZonelessTestEnv } from 'jest-preset-angular/setup-env/zoneless';

setupZonelessTestEnv();
